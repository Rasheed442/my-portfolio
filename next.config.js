/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  // Other configurations...
  future: {
    webpack5: true,
  },
};

module.exports = nextConfig;
